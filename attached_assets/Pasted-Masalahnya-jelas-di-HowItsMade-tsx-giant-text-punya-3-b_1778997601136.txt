Masalahnya jelas — di HowItsMade.tsx, giant text punya 3 baris (Dari, Tikar, Ke Tangan) dengan font size clamp(60px, 10vw, 140px) yang terlalu tinggi. Ini yang bikin layout berantakan.
Ini kode fix langsung — replace seluruh isi src/components/HowItsMade.tsx:
tsximport processMaterial from "@/assets/process-material.jpg";
import processWeaving from "@/assets/process-weaving.jpg";
import processPacked from "@/assets/process-packed.jpg";

const steps = [
  {
    img: processMaterial,
    label: "Dipilih dengan Teliti",
    sub: "Bahan tikar dipilih satu per satu untuk memastikan kualitas dan ketahanan terbaik.",
  },
  {
    img: processWeaving,
    label: "Dianyam dengan Tangan",
    sub: "Setiap tas dianyam secara manual oleh pengrajin lokal, menjadikannya benar-benar unik.",
  },
  {
    img: processPacked,
    label: "Siap untuk Kamu",
    sub: "AlibaBag dikemas dengan rapi dan siap dikirimkan langsung ke tanganmu.",
  },
];

export function HowItsMade() {
  return (
    <section
      id="proses"
      style={{ backgroundColor: "#C0C9EE" }}
      className="overflow-hidden"
    >
      {/* PART A — Giant text LEFT + description RIGHT */}
      <div className="relative mx-auto max-w-7xl px-8 pt-16 pb-10">
        <div
          className="grid items-center"
          style={{ gridTemplateColumns: "58% 42%" }}
        >
          {/* LEFT — giant decorative text, 2 lines only */}
          <div className="overflow-hidden">
            <p
              className="select-none font-black uppercase leading-[0.88] tracking-[-3px] whitespace-nowrap"
              style={{
                fontSize: "clamp(52px, 7.5vw, 112px)",
                color: "#FAFAFA",
                opacity: 0.52,
              }}
            >
              DARI TIKAR
              <br />
              KE TANGANMU
            </p>
          </div>

          {/* RIGHT — heading + paragraph, z above text */}
          <div className="relative z-10 pl-8">
            <h3
              className="text-[17px] font-bold uppercase tracking-wide"
              style={{ color: "#1A1A1A" }}
            >
              Kerajinan di Setiap Langkah
            </h3>
            <p
              className="mt-3 text-[13px] leading-relaxed"
              style={{ color: "#1A1A1A", maxWidth: 340 }}
            >
              Dari pemilihan tikar berkualitas hingga proses anyaman yang
              teliti, setiap AlibaBag dibuat dengan penuh perhatian agar
              sampai ke tanganmu dalam kondisi terbaik.
            </p>
          </div>
        </div>
      </div>

      {/* PART B — 3 cards */}
      <div className="mx-auto max-w-7xl px-8 pb-16">
        <div className="relative grid grid-cols-3 gap-5">
          {steps.map((s, i) => (
            <article
              key={s.label}
              className="relative overflow-hidden rounded-2xl"
              style={{
                backgroundColor: "#FAFAFA",
                boxShadow: "0 4px 20px rgba(0,0,0,0.08)",
              }}
            >
              <img
                src={s.img}
                alt={s.label}
                loading="lazy"
                className="w-full object-cover"
                style={{ height: 210 }}
              />
              <div className="p-4">
                <p
                  className="text-[12px] font-bold uppercase tracking-wide"
                  style={{ color: "#1A1A1A" }}
                >
                  {s.label}
                </p>
                <p
                  className="mt-1.5 text-[12px] leading-relaxed"
                  style={{ color: "#898AC4" }}
                >
                  {s.sub}
                </p>
              </div>

              {/* Badge on last card */}
              {i === steps.length - 1 && (
                <div
                  className="absolute bottom-3 right-3 flex items-center gap-1.5 rounded-full px-3 py-1.5"
                  style={{
                    backgroundColor: "white",
                    boxShadow: "0 2px 10px rgba(0,0,0,0.10)",
                  }}
                >
                  <span className="text-xs">🇮🇩</span>
                  <span
                    className="text-[11px] font-semibold"
                    style={{ color: "#1A1A1A" }}
                  >
                    Local Craft
                  </span>
                </div>
              )}
            </article>
          ))}
        </div>
      </div>
    </section>
  );
}